############################################
# Trimmed requirements for core web app   #
############################################

# --- Flask web app + forms/auth ---
Flask==2.3.3
Flask-Login==0.6.3
Flask-WTF==1.2.2
flask-sock==0.7.0
simple-websocket==1.1.0
python-dotenv==1.0.0

# --- Database (SQLAlchemy only; no Flask-SQLAlchemy used) ---
SQLAlchemy==2.0.42

# --- LLM / orchestration (CrewAI + LangChain OpenAI wrapper) ---
crewai==0.10.0
PyYAML==6.0.2  # langchain-openai will be pulled transitively by crewai

# --- Retrieval & embeddings (FAISS + sentence-transformers) ---
faiss-cpu>=1.8.0
sentence-transformers>=2.3.0
numpy>=1.26.0
huggingface-hub>=0.20.0

# --- Gemini STT engine + VAD ---
google-genai>=0.3.0
webrtcvad==2.0.10

# --- Local audio utilities + optional offline STT helpers ---
SpeechRecognition==3.14.3
pyttsx3==2.99
pydub==0.25.1

# --- Security (password hashing) ---
argon2-cffi==25.1.0

# --- Parsing & language utilities ---
python-docx==1.2.0
langdetect==1.0.9


############################################
# Testing
############################################
pytest==9.0.2


############################################
# Optional / heavy extras (commented out)  #
# Uncomment only if you need them.        #
############################################

# torch==2.7.1
# torchvision==0.22.1
# transformers==4.30.2
# faster-whisper==1.2.0
# gunicorn==21.2.0
# jupyterlab==4.4.5
# notebook==7.4.4
# scipy==1.15.3
# scikit-learn==1.7.1
# matplotlib-inline==0.1.7

